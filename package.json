{
  "name": "dojo-ghl-connector",
  "version": "1.0.0",
  "description": "Shared GHL API connector for all Dojo FS Command custom apps",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "echo 'Library only — no dev server'",
    "build": "mkdir -p dist && echo '<!doctype html><meta charset=utf-8><title>dojo-ghl-connector</title><h1>dojo-ghl-connector</h1><p>This is a shared library, not a website. See README.md.</p>' > dist/index.html"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.44.0"
  }
}
